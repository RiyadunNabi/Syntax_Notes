```cpp
int countV = count(c.begin(), c.end(), 'V');
// Counts how many times 'V' appears in string c
```

```cpp
string result = string(countV, ' ') + a;
// Creates a string of countV spaces, then appends a
```
